CREATE TABLE <DatabaseName>.employee (
    emp_id INT AUTO_INCREMENT PRIMARY KEY,
    emp_name VARCHAR(100) NOT NULL,
    city_name VARCHAR(100),
    email VARCHAR(100),
    department VARCHAR(100)
);